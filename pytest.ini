[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    unit: pure unit tests (no network, no disk)
    integration: multi-module integration tests (no network)
    e2e: full protocol flow tests (no network)
